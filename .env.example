# Environment Variables Template for ExpenseNavigator
# Copy this file to .env and replace with your actual values

# ==========================================
# MongoDB Atlas Connection
# ==========================================
# Example format (DO NOT USE THESE VALUES):
# mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=mongodb+srv://example_user:example_password@example-cluster.xxxxx.mongodb.net/expense_navigator?retryWrites=true&w=majority

# Optional fallback (STANDARD connection string) if your network/DNS blocks SRV lookups.
# Get it from Atlas: Connect -> Drivers -> "Standard connection string" (mongodb://...)
# MONGODB_URI_DIRECT=mongodb://host1:27017,host2:27017,host3:27017/expense_navigator?ssl=true&replicaSet=...&authSource=admin

# ==========================================
# JWT Secret Key
# ==========================================
# Generate a strong random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace-this-with-a-long-random-secret-string

# ==========================================
# Server Configuration
# ==========================================
PORT=5001
NODE_ENV=development

# ==========================================
# One-time DB setup / default admin
# ==========================================
# In production, protect POST /api/setup-db with this token.
# Provide it as header: X-Setup-Token: <value>
SETUP_TOKEN=replace-with-a-long-random-setup-token

# In production, set an explicit admin password for the bootstrap admin.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_EMAIL=admin@expensenavigator.com
DEFAULT_ADMIN_PASSWORD=replace-with-a-strong-admin-password

# ==========================================
# INSTRUCTIONS:
# ==========================================
# 1. Copy this file to .env
# 2. Replace ALL example values with your actual credentials
# 3. NEVER commit .env file to Git (it's in .gitignore)
# 4. For production, set NODE_ENV=production
